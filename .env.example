# Copy this file to `.env` (same folder) and paste your real key after the `=`.
# Get a key from https://console.anthropic.com/ → API Keys.
# Never commit the real .env file — it's already in .gitignore.

ANTHROPIC_API_KEY=
